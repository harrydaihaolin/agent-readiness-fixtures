# noisy-fixture

A fixture that has the basics in place but trips noise checks: ambiguous
naming (`utils.py`, `helpers.py`, `manager.py`), one large generated file,
and no AGENTS.md.

## Test

```bash
make test
```
