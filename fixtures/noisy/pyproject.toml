[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "noisy-fixture"
version = "0.1.0"
description = "Fixture that trips noise checks."
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["src/noisy_fixture"]
